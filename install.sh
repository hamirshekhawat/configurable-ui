#!/bin/bash
# Move to the niceform directory
cd niceform
# Install dependencies
npm install
# Move back to the root directory
cd ..
# Move to the niceform_be directory
cd niceform_be
# Install dependencies
npm install
# Move back to the root directory
cd ..